# 校园杀 v0.1 → v1.0 优化提示词（Claude Code 用）

> 目标：将 校园杀 v0.1（Campus Battle v2.0.206）从基建扎实但玩法缺陷的状态，提升至可玩、可扩展、对齐设计的 v1.0 版本。
> 项目路径：`C:\Users\31184\Desktop\校园杀v0.1\book-to-skill-1.2.0\game\`
> 设计文档：`C:\Users\31184\Desktop\校园杀v0.1\校园杀 规则和人物.docx`、`C:\Users\31184\Desktop\校园杀v0.1\新角色.docx`

---

## 第一章：角色技能对齐（P0 — 最高优先级）

### 1.1 设计文档权威性声明
`校园杀 规则和人物.docx` 是技能唯一权威来源。当前代码中约 63% 的角色技能实现与文档不符，必须逐角色对齐。

### 1.2 逐角色对照修正清单

请逐个角色按以下格式修正（凡代码与文档不一致的，以文档为准）：

---

**刘帮** (STUDY, 4HP)
- ✅ 屁：保持现有实现
- ❌ 战争践踏被动：当前"伤害+1" → 修正为"攻击距离≥2时，与被攻击者间的角色需选择：弃1手牌、弃1装备、或受到1伤害"

---

**亲宝** (STUDY, 4HP)
- ✅ 甲胄：保持现有实现
- ❌ 暴击：当前"25%概率" → 修正为"用杀时进行猜拳判定（石头剪刀布），胜则伤害+1，可叠加其他增伤"

---

**曾子** (STUDY, 4HP)
- ✅ 反伤：保持现有实现
- ❌ 增：当前"回合开始摸1牌" → 修正为"场上有角色打出萝卜（桃/萝卜均可）时，你对除使用者外一名角色使用的杀伤害+1"——需改为被动触发而非摸牌

---

**科比二十四** (STUDY, 4HP)
- ✅ 插嘴：保持现有实现
- ✅ 坠机：保持现有实现
- ❌ 肘击：当前"对相邻角色造成1伤害" → 修正为"可将杀当肘击使用，无法响应闪；若自身装备有5件牌，可指定目标；否则仅相邻角色；每回合1次"

---

**四海王** (STUDY, 4HP)
- ❌ 顺手牵羊：当前为独立主动技能 → 修正为"可将一张杀 + 一张闪当【小偷小摸】（锦囊牌 Steal）使用，取对方1手牌/装备"
- ✅ 毒气：保持但需支持玩家选择目标
- ❌ 墨迹：当前"跳过下回合" → 修正为"在你回合，可以让一位玩家这回合时间减半"

---

**胖斗士** (STUDY, 4HP)
- ❌ 落榜：当前"受伤3次后回复1体力" → 修正为"受伤后本回合免疫伤害（3次），用桃/萝卜则不免疫"
- ❌ 延庆一门：名称/效果需核对 → 修正为"同阵营角色需弃1装备否则受到1伤害"

---

**老人儿** (STUDY, 4HP)
- ❌ 看片：当前"牌堆顶3张获得1张" → 修正为"可查看牌堆顶4张：选2张摸入手中，其余按原顺序放回牌堆顶或牌堆底"
- ❌ 擤鼻涕：当前"攻击距离+1" → 修正为"可将1张闪当杀使用"

---

**犬** (STUDY, 4HP)
- ❌ 隐忍：当前"受伤积累怒气" → 修正为"有女性角色受伤时进行判定：正面则你对其伤害+1；反面则你血量上限+2"
- ❌ 狂化：当前"消耗怒气提升攻击范围" → 修正为"1血时，杀可选择附带任意属性（火/冰/雷/暗）"

---

**砖哥** (STUDY, 4HP)
- ❌ 健身：当前"健身3次后上限+1" → 修正为多阶段累计：
  - 阶段1：使用杀/闪累计4张 → 体力上限+1，回复1体力
  - 阶段2：再累计4张 → 体力上限+1，手牌上限+1
  - 阶段3：再累计4张 → 体力上限+1，杀伤害+1
  - 阶段4：再累计4张 → 体力上限+1，可无限出杀
- ❌ 表演：当前"禁用主动技能" → 修正为"将一张杀或闪背面朝上放置，让对方猜牌名；猜错则：若为杀→目标伤害+1 且不可出闪；若为闪→摸2张牌"
- ❌ 格挡：当前"临时护盾" → 修正为"弃2张牌可抵挡1点伤害，每回合1次"

---

**画师** (STUDY, 3HP)
- ❌ 背刺：当前"30%额外伤害" → 修正为"濒死时可将伤害转移给他人，一回合1次"
- ✅ 望眼欲穿：保持现有实现

---

**双六将** (STUDY, 3HP)
- ❌ 巨根：名称可能需修正，需对照文档 → 实现为"出牌阶段，可至多选择两名其他角色各获得其一张手牌"
- ❌ 折叠：当前"本回合无限出杀" → 修正为"出牌阶段限一次，你可以弃置一张手牌，令你的【杀】造成伤害+1"

---

**沙蛾** (UNIVERSE, 3HP)
- ❌ 狂暴：当前"失去1体力攻击范围+2" → 修正为"在你的回合，可弃一张装备牌，攻击范围+2 且无视防具"
- ✅ 亡魂：保持现有实现

---

**恋旧海** (UNIVERSE, 3HP)
- ✅ 闪电战：保持现有实现
- ❌ 虚化：当前"40%概率免疫伤害" → 修正为"可丢掉一张武器牌免疫一次伤害；主公有虚化技能时，同阵营角色也可丢武器免疫"

---

**黄河黄** (UNIVERSE, 4HP)
- ❌ 养蛊：当前"弃2牌造成2伤害" → 修正为"在你的回合，可用一张杀选择一人，令其与另一人进行一次决斗"
- ✅ 不死图腾：保持现有实现

---

**柳叶** (UNIVERSE, 4HP, 主公)
- ✅ 结盟：保持现有实现
- ✅ 绝望：保持现有逻辑但需验证条件
- ❌ 主公技：修正为"同阵营各摸1牌"

---

**世界霸主** (UNIVERSE, 3HP)
- ❌ 侵略：需对照文档核实 → 保持现有
- ✅ 扩张：保持现有实现
- ❌ 画画：当前"画2次回复1体力" → 修正为"可画一幅画（持续2回合），完成后回复1体力"

---

**梅希** (TEACHER, 3HP)
- ❌ 打压：需对照文档核实 → 修正为"指定一名玩家不能使用技能；也可能使其跳过下回合"
- ❌ 大笑：当前"回复1体力" → 修正为"出牌阶段限一次，回复1点体力"
- ❌ 作业：修正为"指定一位玩家，他需弃1张牌，否则跳过下回合"

---

**柠檬** (TEACHER, 3HP)
- ❌ 搜查：当前"弃置目标所有杀" → 修正为"弃置一名角色区域里的所有杀"
- ❌ 权威：当前"手牌>2敌人弃1牌" → 修正为"让手牌数大于你的角色各弃置一张牌"
- ✅ 真实：保持现有实现

---

**丞相** (TEACHER, 4HP)
- ❌ 请假：当前"跳过回合回复2体力" → 修正为"可替他人出闪，一回合1次"
- ❌ 隐士：当前"免疫锦囊" → 修正为"群体技能无效"
- ❌ 鼓励：需对照文档核实

---

**副主任** (TEACHER, 3HP)
- ❌ 恩赐：当前"所有人摸1牌" → 修正为"弃两张牌，然后让一个人（可为自己）摸2张牌"
- ❌ 利用：当前"获取1手牌" → 修正为"猜拳判定：胜→目标听你指令进行一次操作（出杀/用萝卜）；败→自己弃1张牌"
- ❌ 解放：当前"上限+1回复2体力" → 修正为"回血时，可解除一名玩家身上的debuff"

---

**校长** (TEACHER, 4HP, 主公)
- ❌ 调解：当前"所有角色回复1体力" → 修正为"可转移一个人将要受到的伤害到自己身上，然后自扣1血"
- ✅ 贪污：保持现有实现
- ❌ 阵营技：需对照文档

---

### 1.3 新角色完整实现

**红龙** (UNIVERSE, 4HP)
- 无妄之灾：弃牌阶段，每弃一张牌获得1点标记。标记累计4点后，清除所有标记，给自己下一名玩家施加"闪电"（处分/判定区延迟伤害）
- 甲亢：场上有异性角色时，可在自己回合内弃一张装备牌，本回合杀伤害+1。全局只能使用1次

**兵王** (UNIVERSE, 4HP)
- 酒蒙子：喝酒可翻面，免疫本轮（一回合）所有伤害
- 挖墙脚：指定一名玩家进行判定。正面：将该玩家拉为自己同一阵营；反面失败。从本回合往后每回合需再判定：正面扣1血，反面无事

**重要**：修正 HeroSelectScene 中红龙和兵王同时出现在 UNIVERSE 和 TEACHER 两个阵营的 bug，每个角色只应属于一个阵营。

---

## 第二章：人类玩家交互系统（P0）

### 2.1 出闪/出杀响应交互

当前 `GameEngine.respondShan()` 和 `respondSha()` 对人类玩家自动使用第一张匹配卡牌。需改为回调式交互：

```
流程：
1. GameEngine 检测人类玩家需响应 → 通过 EngineCallbacks 发送 prompt 事件
2. BattleScene 收到 prompt → 显示交互UI（手牌中可用的卡牌高亮）
3. 玩家点击卡牌或"取消" → 回调 GameEngine 继续结算
```

具体修改点：
- `EngineCallbacks` 增加 `onPrompt` 回调
- `respondShan()` / `respondSha()` 改为异步 Promise 模式
- BattleScene 实现 PromptPanel：显示说明文字 + 可用卡牌 + 取消按钮
- 支持超时自动选择（参考回合时间限制）

### 2.2 弃牌阶段 UI

当人类玩家进入弃牌阶段，手牌数 > 当前体力值时：

```
交互要求：
- 高亮显示需要弃牌的数量："需弃置 N 张牌"
- 玩家点击手牌选中（多选），选中卡牌上浮+金色边框
- "确认弃牌"按钮，未选够时禁用
- 实时计数：已选 X / 需弃 N
```

### 2.3 濒死救援交互

当有角色进入濒死状态时：

```
交互要求：
- 全体暂停，显示濒死角色
- 人类玩家手牌中桃/萝卜高亮
- 人类玩家可选择目标出桃救援，或跳过
- 按座次顺序询问，每人限时5秒
```

### 2.4 锦囊目标选择增强

当前锦囊目标选择仅为"点击目标→使用"，缺乏：
- 目标合法性高亮（有效目标亮色，无效目标灰色）
- 技能预览弹窗（展示技能效果描述后再确认）
- 手牌预览（如【搜刮】选目标时显示对方手牌数/装备数）

---

## 第三章：引擎架构重构（P1）

### 3.1 消除硬编码 — 事件总线 + 数据驱动

**问题**：GameEngine 中存在 7+ 处 `if (player.character.defId === 'xxx')` 硬编码。

**方案**：统一通过 SkillResolver 的 ON_TURN_START / ON_CARD_PLAYED 等事件触发。

**重构示例**：

```typescript
// 修改前（GameEngine 硬编码）：
if (player.character.defId === 'shijiebazhu') drawCount += 1;

// 修改后（SkillResolver 注册）：
skillResolver.registerPassive('shijiebazhu', '扩张', SkillTrigger.ON_DRAW_PHASE, (player, state) => {
  return { drawBonus: 1 };
});
```

需要迁移的硬编码点：
1. 世界霸主 扩张（drawCount +1）→ `ON_DRAW_PHASE` 事件
2. 曾子 增（drawCount +1）→ 同上，按修正后的技能逻辑改为胡萝卜关联
3. 刘帮 屁（出萝卜后）→ 已有 resolveActiveSkill，保留
4. 校长 贪污（弃牌跳过判定）→ `ON_DISCARD_PHASE` 事件
5. 柳叶 绝望（手牌上限）→ `getHandLimit` 查询方法
6. 柠檬 真实（闪判定）→ `ON_SHA_RESPONSE` 事件
7. 亲宝 甲胄（伤害减免）→ 已在 DamageSystem，但应迁移到统一的伤害修正链

### 3.2 BattleScene ViewModel 抽取

**当前状态**：BattleScene 直接持有 GameEngine 引用并自行管理状态同步。

**目标架构**：

```
BattleScene (View)
    ↓ 数据绑定 / 事件
BattleViewModel (新文件)
    ↓
GameEngine (Model)
```

**ViewModel 职责**：
- 持有 GameEngine 引用
- 将 GameState 转换为视图友好的数据结构
- 提供 `playCard()` / `useSkill()` / `endTurn()` 等统一命令接口
- 实现 dirty tracking + 增量更新通知
- 管理所有交互状态（selectedCard / selectingTarget / promptQueue）

**BattleScene 职责瘦身**：
- 仅负责渲染：接收 ViewModel 的数据变化通知后刷新对应层
- 仅负责输入：将用户点击/拖拽事件转发给 ViewModel

### 3.3 数据配置外部化

将 `CHAR_DATA` 和技能从代码中分离到 JSON 配置文件：

```
src/data/
├── config.ts         # 平衡常量
├── characters.json   # 23+角色定义（含阵营/血量/技能名/主公标记）
├── skills.json       # 技能详细效果参数
├── cards.json        # 卡牌池定义
└── deck.json         # 初始牌组配比
```

SkillResolver 改为从 JSON 读取技能效果参数，每个技能仅需注册通用 handler 类型而非每个角色单独写 handler。

---

## 第四章：锦囊与装备补全（P2）

### 4.1 锦囊效果对齐清单

| 锦囊 | 需修正内容 |
|---|---|
| **处分（闪电）** | 改为标准判定区机制：放入→每回合判定→反2-3次后3雷伤→否则转移下家 |
| **战争践踏** | 无闪时支持弃装备（当前仅弃手牌） |
| **假条** | 实现实际抵消逻辑：在锦囊生效前询问是否使用假条 |
| **臭水炸弹** | 修正：下个自己的回合爆炸，其他所有玩家血量**归零**（0 HP 而非1 HP），进入濒死结算 |
| **福同享难同当** | 实现伤害共享：sharePairs 中的角色受到伤害时，伙伴也受到同等伤害 |
| **命运之矛** | 修正：造成伤害后标记，目标**下回合开始**才掉血 |

### 4.2 装备效果对齐清单

| 装备 | 需修正内容 |
|---|---|
| **岁月刀** | 新增效果：任意2张手牌可当杀使用（当前仅有+1攻击距离） |
| **臭水炸弹** | 从装备区移除后应取消爆炸计时 |
| **钰鞋（玉鞋）** | 途经角色伤害当前由 DistanceCalculator 支持，但 BattleScene 未显示途经角色提示 |
| **粉笔盒** | 无限出杀已实现，但需在 UI 中醒目提示（如边框变金） |

### 4.3 角色专属牌

文档中提到"角色专属牌"概念（仅对应角色可获得的独特卡牌）。当前完全未实现。需：
- 在 CardManager 中实现专属牌模块
- 每个角色定义 1-2 张专属牌（如刘帮的"战争践踏专属版"）
- 发牌时加入概率判定

---

## 第五章：代码质量与工程化（P2）

### 5.1 TypeScript 严格模式

`tsconfig.json` 中启用：
```json
{
  "strict": true,
  "noImplicitAny": true,
  "strictNullChecks": true,
  "noUnusedLocals": true,
  "noUnusedParameters": true
}
```

然后修复所有编译错误。重点关注：
- `DamageSystem.dealDamage()` 的 `source` 参数允许 null 但不一致
- `RuntimeCard` 中 `tacticType` / `equipType` 等可选字段的安全访问

### 5.2 单元测试覆盖

优先测试（因为出 bug 影响最大）：
1. `DamageSystem`：伤害链、濒死结算、游戏结束判定
2. `CardManager`：抽牌/弃牌/洗牌边界条件
3. `JudgeSystem`：硬币/猜拳概率验证
4. `DistanceCalculator`：环形距离/装备修正边界

### 5.3 自动化回归测试

`AutoValidator.ts` 增强：
- 全角色技能矩阵测试：每个角色 vs 每个角色的模拟对战
- 卡牌效果组合测试（装备+锦囊交互）
- 边界条件测试（空牌堆重洗、满血用桃、空手牌弃牌）

### 5.4 Python 版本决策

**方案 A（推荐）**：废弃 Python 版，在 README 中注明 TS 版为正式版本。
**方案 B**：维护 Python 版作为规则验证沙盒，从共享 JSON 配置读取角色/卡牌数据。

建议选择方案 A，除非 Python 版有特殊用途（如服务器端规则引擎）。

---

## 第六章：联网对战集成（P2）

### 6.1 客户端接入服务端

当前 `server/` 目录有完整的 Socket.io 服务端实现，但 BattleScene 完全未接入。

集成步骤如下：
1. 在 GameBoot 中增加网络模式检测（URL 参数 ?mode=online）
2. HeroSelectScene 增加"创建房间"/"加入房间"选项
3. BattleScene 注入 NetworkClient 替代本地 GameEngine
4. 实现网络状态处理：
   - 连接断开 → 暂停游戏 + 重连倒计时
   - 对手超时 → 自动托管给 AI
   - 重连成功 → EventReplayer 回放期间的 actionLog

### 6.2 服务端/客户端引擎统一

当前 `server/src/core/GameEngine.ts`（14KB）和 `src/engine/GameEngine.ts`（31KB）是两套独立实现。

**方案**：抽取共享游戏逻辑包 `@campus-battle/core`，包含：
- `core/types.ts`：全部类型定义
- `core/rules/`：GameEngine / SkillResolver / DamageSystem / CardManager
- `core/judge/`：JudgeSystem / DistanceCalculator

前后端共同引用，消除重复代码。

---

## 第七章：UI/UX 增强（P3）

### 7.1 Canvas → WebGL

将 Phaser 配置从 `Phaser.CANVAS` 改为 `Phaser.WEBGL`，启用：
- 高级着色器特效（卡牌发光、全屏滤镜）
- 硬件加速粒子系统
- 更流畅的动画（当前部分动画有掉帧）

迁移注意事项：
- 所有 Graphics 绘制不受影响
- 粒子系统需验证 WebGL 模式兼容性
- 移动端电容式触摸输入需重新测试

### 7.2 音效系统完善

当前 SoundManager 只有骨架。需要：
1. 收集/制作音效资源（出牌/攻击/技能/胜利/失败/回合切换）
2. 实现音频预加载（BootScene 中加载）
3. 音量控制（设置菜单）

### 7.3 卡牌动画增强

当前卡牌使用/打出的动画较简化。建议增加：
- 杀 → 卡牌飞向目标 + 命中震动 + 红色屏幕闪
- 桃/萝卜 → 绿色治疗光环 + 血量数字跳动
- 锦囊 → 卡牌翻转 + 粒子爆发
- 死亡 → 卡牌碎裂 + 角色渐隐

### 7.4 响应式适配

当前锁定 1600×720（21:9 宽屏），在 16:9 或竖屏设备上体验差。

建议：
- 使用 ScreenAdapter 实现多比例适配
- 竖屏模式（手机）：改为三栏布局（我方/公共区/敌方）
- 平板模式：16:9 或 4:3 布局

---

## 第八章：执行顺序建议

按依赖关系和收益排序，建议按以下批次执行：

### 第一批（第1-2周）：P0 核心修复
1. 角色技能对齐（第一章全部23人）
2. 人类玩家出闪/出杀交互（第二章 2.1）
3. 弃牌阶段/濒死救援UI（第二章 2.2/2.3）
4. 新角色完整实现（第一章 1.3）

### 第二批（第3-4周）：P1 架构重构
5. 引擎硬编码消除（第三章 3.1）
6. BattleScene ViewModel 抽取（第三章 3.2）
7. 数据配置外部化（第三章 3.3）

### 第三批（第5-6周）：P2 补全
8. 锦囊/装备效果对齐（第四章）
9. 单元测试 + 自动化回归（第五章 5.2/5.3）
10. TypeScript 严格模式（第五章 5.1）

### 第四批（第7-8周）：P2/P3 联网与体验
11. 客户端接入服务端（第六章 6.1）
12. 前后端引擎统一（第六章 6.2）
13. Canvas→WebGL + 音效 + 动画增强（第七章）

---

## 附录A：设计文档关键规则速查

- **阵营**：学阵营(STUDY) / 宇阵营(UNIVERSE) / 师阵营(TEACHER)
- **胜利条件**：消灭其他阵营所有角色
- **主公**：柳叶(宇)、校长(师)，体力+1，有主公技
- **回合流程**：准备→判定→摸牌2→出牌→弃牌（手牌≤当前体力）→结束
- **卡池**：30杀/15闪/8桃/3萝卜/36锦囊/8装备 ≈ 100张
- **玩家数**：2-4人本地；联网模式目标4人及以上

## 附录B：关键文件索引

| 文件 | 路径 | 用途 |
|---|---|---|
| 设计文档 | `..\校园杀 规则和人物.docx` | 技能权威来源 |
| 新角色设计 | `..\新角色.docx` | 红龙/兵王设计 |
| Python版源码 | `..\campus_kill.py` | 参考实现 |
| 游戏引擎 | `src\engine\GameEngine.ts` | 回合/卡牌/AI 核心 |
| 技能解析 | `src\engine\SkillResolver.ts` | 23+角色技能实现 |
| 战斗场景 | `src\scenes\BattleScene.ts` | 渲染+交互 |
| 架构规范 | `CLAUDE.md` | MVVM 铁律 |
| 类型定义 | `src\core\types.ts` | 静态类型 |
| 运行时类型 | `src\engine\types.ts` | 动态状态类型 |

---

> 生成时间：2026-07-26
> 预计总工时：约 8 周（按单人全职）
> 目标状态：v1.0 可玩版本（单人 + 联网对战，技能100%对齐设计文档）
*（内容由AI生成，仅供参考）*
